# Extra dependencies for the signal engine on top of the root
# requirements.txt (which stays lean for the hourly sync job).
# The engine needs pandas for the shared 1h->4h resample in backtest/loader.
pandas>=2.2
